import React from "react"

const App = ({ store }) => (
    <div>
        main
        {/* <Header addTodo={store.addTodo} />
        <MainSection store={store} /> */}
    </div>
)

export default App
